import Foundation

enum CornerRadius {
    /// No corner radius (0 points)
    static let none: CGFloat = 0

    /// Small corner radius (8 points)
    static let small: CGFloat = 8

    /// Medium corner radius (16 points)
    static let medium: CGFloat = 16
}
