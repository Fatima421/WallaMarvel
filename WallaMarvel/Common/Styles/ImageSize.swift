import Foundation

enum ImageSize {
    /// Small size (80 points)
    static let small: CGFloat = 80

    /// Medium size (150 points)
    static let medium: CGFloat = 150

    /// Large size (350 points)
    static let large: CGFloat = 350

    /// Offset value (-30)
    static let offsetValue: CGFloat = -30
}
